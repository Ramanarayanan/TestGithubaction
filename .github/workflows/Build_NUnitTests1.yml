name: Build_NUnitTests1
on:
  push:
    branches:
      - refs/heads/main
  workflow_run:
    workflows:
      - Build_Dev
    types:
      - requested
jobs:
  build:
   runs-on: ubuntu-latest
   steps:
    - name: BuildStep_NUnitTests
      run: PS1
env: {}
